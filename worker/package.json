{
  "name": "@bussey/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: API + Claude orchestration + Stripe webhooks for Bussey and Bussey.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint: linter not yet configured for worker' && exit 0",
    "seed:bootstrap-admin": "node scripts/seed-bootstrap-admin.mjs"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.92.0"
  }
}
