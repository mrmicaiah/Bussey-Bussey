<script lang="ts">
  // Retired surface (Calls layer step 6). The +page.ts load redirects to /calls
  // before this ever renders; this is only a fallback if a redirect is skipped.
</script>

<svelte:head><title>Redirecting · Studio44</title></svelte:head>

<p class="muted">Redirecting to Calls…</p>
