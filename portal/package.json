{
  "name": "@bussey/portal",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Client portal SPA (SvelteKit) — first-login walkthrough, documents, payment, project status.",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --threshold error",
    "lint": "echo 'lint: linter not yet configured for portal' && exit 0"
  },
  "dependencies": {
    "@stripe/stripe-js": "^4.0.0",
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.6",
    "@sveltejs/kit": "^2.8.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@types/node": "^25.9.0",
    "svelte": "^5.1.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
